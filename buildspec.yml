version: 0.1

phases:
  build:
    commands:
      - sh ./gradlew --no-daemon test shadowJar
      - aws cloudformation package --template-file sam_template.yml --s3-bucket codepipeline-us-east-1-474657647076 --output-template-file NewSamTemplate.yaml

artifacts:
  files:
    - NewSamTemplate.yaml
